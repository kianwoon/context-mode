{
  "name": "context-mode",
  "version": "2.5.0",
  "description": "Sandboxed code execution + FTS5 knowledge base. 5 tools: execute, batch_execute, search, get_chunk, fetch_and_index.",
  "mcpServers": {
    "context-mode": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/build/index.js"]
    }
  }
}
